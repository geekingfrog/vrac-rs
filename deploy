#!/usr/bin/env bash

set -euo pipefail

rsync --progress --recursive \
  migrations \
  templates \
  ./target/release/vrac \
  ./target/release/cleanup \
  ./target/release/gen-user \
  Rocket.toml \
  linode:~/vrac/
